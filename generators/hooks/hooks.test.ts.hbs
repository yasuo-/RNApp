import { act, renderHook } from '@testing-library/react-hooks'

import { {{camelCase name}} } from '../../src/hooks/{{camelCase name}}'

describe('{{camelCase name}} hook', () => {
    it('should ...', () => {
        // expect().toBe()
    })
})