import { useEffect, useState } from 'react';

/**
 * {{camelCase name}}
 *
 * @example
 * ```ts
 *  const {} = {{camelCase name}}()
 * ```
 * @category ReactHooks
 */
export const {{camelCase name}} = props => {
  return {}
};
