import React from 'react';
import {Text, SafeAreaView, View} from 'react-native';

// import {useNavigation} from '@react-navigation/native';

export const ChooseScreen = (props: any) => {
  console.log(props);
  return (
    <SafeAreaView style={[{flex: 1, backgroundColor: '#FFF'}]}>
      <View>
        <Text>dddd</Text>
      </View>
    </SafeAreaView>
  );
};
